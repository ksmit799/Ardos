[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
# Every test gets a hard timeout so a stuck daemon doesn't hang CI.
timeout = 60
timeout_method = thread
filterwarnings =
    ignore::DeprecationWarning
markers =
    benchmark: pytest-benchmark scenarios (run explicitly).
